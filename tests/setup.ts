// Loaded by Jest via setupFilesAfterEnv. Importing here (in a file tsconfig includes)
// also registers the jest-dom matcher types for tsc, so `next build` type-checks the tests.
import '@testing-library/jest-dom';
